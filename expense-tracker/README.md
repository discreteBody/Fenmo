# Expense Tracker (frontend)

This folder contains the React + Vite frontend for Fenmo — Expense Tracker.

Quick start

```bash
cd expense-tracker
npm install
npm run dev
```

Notes
- The frontend expects the API at `VITE_API_BASE` (default: `/expenses`).
- To build a production bundle: `npm run build`.
- Key files: `src/ExpenseTracker.jsx`, `src/ExpenseForm.jsx`, `src/ExpenseTable.jsx`, `src/index.css`.
